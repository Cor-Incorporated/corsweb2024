# ADR-0005 Contact を段階移行（Phase1 SSGFORM 継続、Phase2 Cloudia UI + contact-chat Worker・LLM 非依存）

## ステータス: Accepted (2026-06-13) / **改訂 2026-07-10**（実装実態・Cloudia 統合方針を反映） / **改訂 2026-09-13**（§6 フォールバック必須を撤回し Cloudia 一本化）

## 背景
- 当初 `ContactForm.astro` は外部フォーム SSGFORM に PII を送信。Phase1 で同意導線・PP 明記・相談種別更新を実施済み（または進行中）。
- 2026-06 時点の ADR は Phase2 を「GCP Cloud Run の自社チャットボット」と記載していたが、実装は **Cloudflare Workers `workers/contact-chat`**（`cor-jp.com/api/contact/*`）に収束している。
- 2026-07-10 監査（`Cor_Grift_サイト刷新提案`）により、Contact の最終 UI は **Cloudia（3d-emotional-chat-ai）をフォーム代用のチャットボット** とし、同一オリジンで corsweb に統合する方針が確定した。
- 3D 表示不具合・別ドメイン・Netlify 配置・個人リポ（`terisuke`）のままでは B2B 受付に使えない。

## 決定

### Phase1（維持）
- ~~SSGFORM 継続は **fallback / JS 無効時の最小連絡手段** として残す。~~ → **撤回（2026-09-13 改訂）**。§6 と同時に撤回。最小連絡手段は残さない（下記「2026-09-13 改訂」参照）。
- 利用目的明示・PP リンク・同意導線・現行事業の相談種別を維持。

### Phase2（改訂後の正本）
Contact の本線を次の二層で構成する。

| 層 | 正本 | 役割 |
|---|---|---|
| **API** | `workers/contact-chat`（Cloudflare Worker） | `/api/contact/chat`（会話・分類・PII 非保存）・`/api/contact/submit`（PII はメールのみ・LLM 非経由） |
| **UI** | Cloudia（旧 3d-emotional-chat-ai） | お問い合わせフォーム代用のチャット UI。3D アバターは任意・失敗時はテキストのみで完走 |

追加決定:

1. **GCP Cloud Run は Phase2 の前提から外す**（本 ADR の 2026-06 記載を撤回）。
2. **LLM プロバイダ非依存**は維持（現状 Anthropic 既定・抽象化レイヤで他プロバイダ追加可）。
3. **intent 初期化**: `?intent=confidential-ai-assessment|local-llm-poc|grift-team-beta|grift-paid-trial|estimate-audit|press-speaking-other` を受け取り、初回選択肢をプリセット。
4. **構造化リード**: 目的 → 業種・役割 → データ機密度（具体本文は入力させない）→ 進捗段階 → 時期・予算帯 → 連絡先・同意 → 要約の利用者修正 → 送信。
5. **記録方針**: 生会話全文の無期限保存はしない。要約と必要項目を主記録。全文は短期または非保存（PP と整合、ADR-0012 参照）。
6. **~~フォールバック必須~~ → 撤回（2026-09-13 改訂）**: 旧「JS 無効・Cloudia 障害時はメール/電話/最小フォーム」を撤回し、**`/contact` の問い合わせ導線を Cloudia（AI チャット）へ一本化**する。3D 緑画面等でもチャット本体は動作。詳細は「## 2026-09-13 改訂: 問い合わせ導線の Cloudia 一本化」を参照。
7. **統合ロードマップ**（詳細は ADR-0012）:
   - A: B2B 受付 UI・intent（現行 Cloudia リポ）
   - B: GitHub Transfer → `Cor-Incorporated`
   - C: Netlify → Cloudflare
   - D: corsweb `/contact/` 主 UI = Cloudia カットオーバー

### 受付トーン
- Contact / B2B 受付: **敬語**（「おっす！」等の SNS 口調は使わない）。
- SNS アンバサダー用途は別モードとして分離可。

## 理由
- 実装済みの contact-chat Worker を捨てて GCP に寄せ直すコストが高く、PII を「メールのみ・LLM 非経由」にできる現状設計が ADR 当初の越境最小化意図と整合する。
- 監査で「無料相談・汎用フォーム・別ドメイン Cloudia」が導線の真実性を損なっているため、**同一ドメインの構造化チャット**が公開前〜公開直後の本線になる。
- ~~フォーム即廃止は a11y / 障害時リスクがあるため fallback を残す。~~ → **撤回（2026-09-13 改訂）**。この a11y / 障害時リスクは認識したうえで、Cloudia 一本化の判断により受容した（下記「2026-09-13 改訂」参照）。

## 影響
- Issue #59 および関連 Issue を本改訂に合わせて更新する。
- `CONTACT_CHAT_ENABLED` や Contact ページは、最終的に ~~**Cloudia 主 UI + フォーム fallback**~~ → **Cloudia 主 UI のみ（フォーム fallback は 2026-09-13 改訂で撤回）** に置き換える（中間段階では既存 ContactChat ウィジェットを API 接続の試験に使ってよい）。
- PP を contact-chat / Cloudia の処理フローに合わせて再更新する。
- チャットボット PII は CMS DB（ADR-0003 Supabase）に置かない（従来どおり）。
- Cloudia リポ移管・CF ホスティングは ADR-0012 / Cloudia 側 ADR-0004 に従う。

## 代替案
- **GCP Cloud Run に寄せ直す**: 既存 Worker と二重投資になるため却下。
- **SSGFORM を本線のまま据え置き**: 構造化リード・intent・同一ドメイン体験が得られず、監査 P0 を満たせないため却下。
- **3D なしのテキストウィジェットのみ**: 短期は可。最終ブランド UI としては Cloudia 統合を目標に残す。

## 参照
- ADR-0012 Cloudia 統合・org 移管・CF
- 監査資料: `Cor_Grift_サイト刷新提案_2026-07-10.md` §6, §12
- `workers/contact-chat/README.md`

## 2026-09-13 改訂: 問い合わせ導線の Cloudia 一本化

### 決定
`/contact`（全5言語）から電話番号を取り下げ、**問い合わせ導線を Cloudia（AI チャット）に一本化**する。これに伴い §6 の「フォールバック必須（メール/電話/最小フォーム）」を**撤回**する。

- 電話カードは `CALL_CENTER_PHONE_VISIBLE`（`src/config/contact.ts`）で描画を止める。番号自体と `CALL_CENTER_TEL_HREF` は残しており、この定数を `true` に戻せば復帰できる。
- 「電話または日程調整へ」と案内していた文言（`CloudiaContactEntry` / `CloudiaLauncher` / `/contact/chat`、および全5言語の `calendar.note`）は、実在しない手段を案内しないよう書き換えた。
- プライバシーポリシーの苦情受付経路からも電話を削除（全5言語）。開示請求等のメール窓口 `company@cor-jp.com` は従来どおり維持。

### 判断者・経緯
諫山（PM、2026-09-13）。「問い合わせはクラウディアの AI チャットに寄せたい」との方針決定による。ADR §6 との衝突を提示したうえで、フォールバックを持たない選択を明示的に採用した。

### 受容したリスク
- **JS 無効環境および Cloudia 障害時、`/contact` 上に問い合わせ手段が存在しない。** Cloudia は JS 前提の SPA であり、かつ本リポジトリ外で別途デプロイされているため、本リポジトリのビルドでは可用性を担保できない。
- 障害は利用者側から不可視で、問い合わせが静かに取りこぼされうる（サイト側にエラー表示は出ない）。

### 検知手段（撤回条件を発動可能にするため必須）
受容したリスクは「利用者側から不可視」であり、検知手段を伴わない撤回条件は永久に発動しない。
そのため以下を撤回条件とセットで定める。

1. **合成監視**: `https://cor-jp.com/contact/chat/` を定期取得し、本文に `AIチャットを準備しています`
   （en: `The AI chat is unavailable`）が含まれたら Cloudia SPA が配信されていない＝
   サイト上の問い合わせ手段がゼロの状態。即アラートする。この文字列は本リポジトリの
   プレースホルダ固有であり、SPA が正常配信されていれば出現しない。
2. **流量監視**: Cloudia 経由の受信が営業時間 N 時間ゼロならアラート（閾値は運用で決める）。
3. JS 無効流入は Clarity / CF ビーコンでは原理的に測れない（JS が動かないためビーコンが飛ばない）。
   代替指標を用意できない限り、これを撤回条件の根拠にはしない。

**所有者**: 諫山（PM）。**定期見直し: 2026-12-13**（トリガー未発動でも決定を再評価する）。

### 撤回条件（下記いずれかに該当したら §6 を復活させる）
- 上記1（合成監視）が発報した場合。
- 上記2（流量監視）で受信ゼロが継続した場合。
- 2026-12-13 の定期見直しで、一本化の継続が不適当と判断された場合。

その際は `CALL_CENTER_PHONE_VISIBLE=true` に戻すか、メール導線または最小フォームを `/contact` に追加する。

### 未実施（本 PR のスコープ外・要対応）
上記の合成監視・流量監視は**まだ設定されていない**。設定されるまでは、撤回条件は事実上
発動しない状態が続く点に注意する。

- 監視の設定: **issue #322**
- `/contact` の回帰ガード + vitest の CI 組み込み: **issue #323**

（本節は #322 の完了時に更新すること。）

### 特定商取引法との関係
`/legal/tokushoho`（全5言語・全ページのフッターからリンク）の電話番号は、特定商取引法上の表示義務があるため**維持する**。したがって本決定は「`/contact` に電話を出さない」ことであり、**電話番号自体はサイト上で引き続き公開されている**。
